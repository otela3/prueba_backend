version: '3.3'

services:
  postgres:
    image: postgres
    container_name: postgres
    environment: 
      - "POSTGRES_USER=myuser"
      - "POSTGRES_PASSWORD=password"
    ports:
      - "5432:5432"
    volumes:
      - "./sql:/docker-entrypoint-initdb.d"
